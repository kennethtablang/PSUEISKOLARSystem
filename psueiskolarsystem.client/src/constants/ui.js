// Shared inline style for compact filter controls on list pages
// (previously duplicated in several pages as `ctlStyle`).
export const ctlStyle = { height: 36, minHeight: 36, fontSize: 12.5, padding: '0 10px' };
